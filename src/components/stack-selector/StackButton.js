import React from 'react';
import Button from '../ui/Button';

const StackButton = ({ name }) => (
  <Button className="btn-outline p0 black regular h6">{name}</Button>
);

export default StackButton;
